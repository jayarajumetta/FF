{
  "version": "39.0.0",
  "rules": [
    "Business Gherkin only; no TBox/system steps in Features",
    "Hooks own browser lifecycle/evidence only",
    "Explicit feature-scoped bindings; no catch-all bindings",
    "StepDefinitions call PageMethods directly in generated source order",
    "PageMethods reference centralized locator constants",
    "Scenario JSON owns static data; random data cached once per scenario key",
    "Synthetic external/protected values fail explicitly",
    "Source conditions are evaluated in PageMethods, not exposed as noisy Gherkin"
  ]
}