# Schemas

`scenario-plan.schema.json` documents the immutable ordered plan contract. Feature-specific data files are validated structurally by `tools/validate_framework.py` and loaded defensively by `StaticDataStore`.
